Generated by `npm run kb:browser` (also `prebuild`):

| File | Role |
|------|------|
| `browser_kb_v1.json` | Slim records + MiniLM document vectors (`hybrid_minilm_keyword_v1`) |
| `browser_kb_meta.json` | Size / counts / notes |
| `runtime.mjs` | Browser ESM retrieval + evidence (~30KB); loads MiniLM from CDN |

Do not hand-edit JSON. Canonical sources remain `public/assets/*.json` → normalizer → this artifact.
